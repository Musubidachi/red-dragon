/**
 * Backtest value objects: frame (candidate + bars), outcome (full pipeline
 * output for one frame), metrics (verdict distribution and aggregates), and
 * report (the wrapping result returned to callers).
 */
package dev.reddragon.backtest.models;
