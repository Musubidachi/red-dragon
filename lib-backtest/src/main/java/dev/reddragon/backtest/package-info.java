/**
 * MD Layer 8 support — deterministic replay harness for historical candidates
 * and bars. See {@code lib-backtest/README.md} for the data flow and the
 * {@code ../ARCHITECTURE.md} mapping.
 */
package dev.reddragon.backtest;
