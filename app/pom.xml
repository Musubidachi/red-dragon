<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0
                             https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>dev.reddragon</groupId>
        <artifactId>red-dragon-parent</artifactId>
        <version>0.0.1-SNAPSHOT</version>
    </parent>

    <artifactId>app</artifactId>
    <packaging>jar</packaging>
    <name>red-dragon-app</name>
    <description>Bootable Spring Boot application; wires the lib-* modules together.</description>

    <dependencies>
        <!-- internal libs -->
        <dependency>
            <groupId>dev.reddragon</groupId>
            <artifactId>lib-domain</artifactId>
        </dependency>
        <dependency>
            <groupId>dev.reddragon</groupId>
            <artifactId>lib-ingestion</artifactId>
        </dependency>
        <dependency>
            <groupId>dev.reddragon</groupId>
            <artifactId>lib-marketdata</artifactId>
        </dependency>
        <dependency>
            <groupId>dev.reddragon</groupId>
            <artifactId>lib-analytics</artifactId>
        </dependency>
        <dependency>
            <groupId>dev.reddragon</groupId>
            <artifactId>lib-validation</artifactId>
        </dependency>
        <dependency>
            <groupId>dev.reddragon</groupId>
            <artifactId>lib-persistence</artifactId>
        </dependency>
        <dependency>
            <groupId>dev.reddragon</groupId>
            <artifactId>lib-backtest</artifactId>
        </dependency>

        <!-- spring -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-web</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-actuator</artifactId>
        </dependency>
        <dependency>
            <!-- Required so @Timed annotations on Spring-managed beans get
                 woven into Micrometer timers. -->
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-aop</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-data-jpa</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-validation</artifactId>
        </dependency>
        <dependency>
            <groupId>org.flywaydb</groupId>
            <artifactId>flyway-core</artifactId>
        </dependency>
        <dependency>
            <groupId>com.h2database</groupId>
            <artifactId>h2</artifactId>
            <scope>runtime</scope>
        </dependency>
        <dependency>
            <groupId>org.postgresql</groupId>
            <artifactId>postgresql</artifactId>
            <scope>runtime</scope>
        </dependency>
        <dependency>
            <groupId>org.projectlombok</groupId>
            <artifactId>lombok</artifactId>
            <scope>provided</scope>
        </dependency>

        <!-- test -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-test</artifactId>
            <scope>test</scope>
        </dependency>
    </dependencies>

    <build>
        <plugins>
            <plugin>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-maven-plugin</artifactId>
                <executions>
                    <execution>
                        <goals>
                            <goal>repackage</goal>
                        </goals>
                    </execution>
                </executions>
            </plugin>
        </plugins>
    </build>
</project>
