spring:
  h2:
    console:
      enabled: true
      path: /h2-console
      settings:
        web-allow-others: false

logging:
  level:
    root: DEBUG
    dev.reddragon: DEBUG
    org.springframework.web: DEBUG
    org.hibernate.SQL: DEBUG
